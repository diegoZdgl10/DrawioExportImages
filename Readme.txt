To run "export.sh" you must perform:
$ bash export.sh <Filename> <output extension>

Supported output extension:
png / jpg / jpeg

Example:
$ bash export.sh Grafica png

The output will be the n images of the input file and the input file in .xml format